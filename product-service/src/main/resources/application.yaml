server:
  port: 5000
logging:
  level:
    org.springframework.web: "debug"
    org.hibernate: "error"
  file:
    name: "productservice.log"
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: Java
